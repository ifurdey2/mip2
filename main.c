#include <stdio.h>
#include <stdlib.h>
#include "Tlist.h"
#define MAXLENSTR (255)
int main()
{
   Tlist L={NULL,NULL};
   char ch;
   int n;
   realty buf;
   char str[MAXLENSTR];
   while (1){
       printf("> ");
       scanf("%c",&ch);
       while (getchar() != '\n');
       switch(ch){
               
    return 0;
}
